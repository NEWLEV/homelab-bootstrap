{
    admin off
    auto_https off
}

http://:18080 {
    bind 127.0.0.1

    # n8n publishes its browser paths under /n8n/, but its REST API is upstream at /rest/.
    handle_path /n8n/* {
        reverse_proxy 127.0.0.1:5678
    }

    handle {
        reverse_proxy 127.0.0.1:18789
    }
}