diff --git a/services/local-rag/app/main.py b/services/local-rag/app/main.py
--- a/services/local-rag/app/main.py
+++ b/services/local-rag/app/main.py
@@ -32,7 +32,7 @@ from app.streaming import sse_event, stream_ollama_answer
 
 
-APP_VERSION = "0.9.1"
+APP_VERSION = "0.9.2"
 
 
 app = FastAPI(
@@ -95,9 +95,7 @@ chroma_client = chromadb.PersistentClient(
 
 collection = chroma_client.get_or_create_collection(
     name="homelab_bootstrap",
-    metadata={
-        "description": "Aisha homelab repository",
-    },
+    metadata=collection_metadata(EMBEDDING_MODEL),
 )
@@ -522,6 +520,7 @@ def health() -> dict[str, Any]:
         "index_status": index_job["status"],
         "last_index_success_at": index_job["last_success_at"],
+        "index_schema_version": INDEX_SCHEMA_VERSION,
     }
@@ -931,9 +930,20 @@ def index_status() -> dict[str, Any]:
     return index_job_manager.snapshot()
 
 
+@app.get("/index/integrity")
+def index_integrity() -> dict[str, Any]:
+    return inspect_collection(
+        collection,
+        chunking_version=CHUNKING_VERSION,
+        embedding_model=EMBEDDING_MODEL,
+    )
+
+
 @app.post("/index")
-def index_documents() -> dict[str, Any]:
+def index_documents(rebuild: bool = False) -> dict[str, Any]:
     from app.indexer import index_repository
@@ -946,7 +956,7 @@ def index_documents() -> dict[str, Any]:
 
     try:
-        result = index_repository()
+        result = index_repository(rebuild=rebuild)
