diff --git a/services/local-rag/tests/test_api.py b/services/local-rag/tests/test_api.py
--- a/services/local-rag/tests/test_api.py
+++ b/services/local-rag/tests/test_api.py
@@ -14,7 +14,7 @@ def test_health() -> None:
     body = response.json()
 
     assert body["status"] == "ok"
-    assert body["version"] == "0.9.1"
+    assert body["version"] == "0.9.2"
@@ -26,6 +26,7 @@ def test_health() -> None:
     assert body["index_status"] in {"idle", "failed"}
     assert "last_index_success_at" in body
+    assert body["index_schema_version"] == "1"
diff --git a/services/local-rag/tests/test_indexer.py b/services/local-rag/tests/test_indexer.py
--- a/services/local-rag/tests/test_indexer.py
+++ b/services/local-rag/tests/test_indexer.py
@@ -12,6 +12,7 @@ class FakeCollection:
     def __init__(self) -> None:
         self.records: dict[str, dict[str, Any]] = {}
+        self.metadata: dict[str, str] = {}
@@ -45,6 +46,9 @@ class FakeCollection:
     def count(self) -> int:
         return len(self.records)
 
+    def modify(self, metadata: dict[str, str]) -> None:
+        self.metadata = metadata
+
 
 class FakeClient:
@@ -59,6 +63,8 @@ class FakeClient:
     ) -> FakeCollection:
         assert name == "homelab_bootstrap"
         assert metadata["description"] == "Aisha homelab repository"
+        if not self.collection.metadata:
+            self.collection.metadata = metadata
         return self.collection
@@ -338,3 +344,78 @@ def test_chunking_version_change_reindexes_unchanged_file(
         for record in collection.records.values()
     )
+
+
+def test_schema_metadata_is_stored_on_chunks(
+    tmp_path: Path,
+    monkeypatch,
+) -> None:
+    source_root = tmp_path / "repository"
+    source_root.mkdir()
+    (source_root / "README.md").write_text(
+        "Documentation",
+        encoding="utf-8",
+    )
+    collection = FakeCollection()
+    configure_indexer(monkeypatch, source_root, collection)
+
+    indexer.index_repository()
+
+    metadata = next(iter(collection.records.values()))["metadata"]
+    assert metadata["schema_version"] == indexer.INDEX_SCHEMA_VERSION
+    assert metadata["embedding_model"] == indexer.EMBEDDING_MODEL
+    assert collection.metadata["schema_version"] == (
+        indexer.INDEX_SCHEMA_VERSION
+    )
+
+
+def test_embedding_model_change_reindexes_unchanged_file(
+    tmp_path: Path,
+    monkeypatch,
+) -> None:
+    source_root = tmp_path / "repository"
+    source_root.mkdir()
+    (source_root / "README.md").write_text(
+        "Documentation",
+        encoding="utf-8",
+    )
+    collection = FakeCollection()
+    configure_indexer(monkeypatch, source_root, collection)
+    indexer.index_repository()
+
+    monkeypatch.setattr(indexer, "EMBEDDING_MODEL", "replacement-model")
+    result = indexer.index_repository()
+
+    assert result["metadata_migrated_files"] == 1
+    assert result["updated_chunks"] == 1
+    assert all(
+        record["metadata"]["embedding_model"] == "replacement-model"
+        for record in collection.records.values()
+    )
+
+
+def test_explicit_rebuild_reembeds_unchanged_files(
+    tmp_path: Path,
+    monkeypatch,
+) -> None:
+    source_root = tmp_path / "repository"
+    source_root.mkdir()
+    (source_root / "README.md").write_text(
+        "Documentation",
+        encoding="utf-8",
+    )
+    collection = FakeCollection()
+    embed_calls: list[str] = []
+    configure_indexer(monkeypatch, source_root, collection)
+    monkeypatch.setattr(
+        indexer,
+        "embed",
+        lambda text: embed_calls.append(text) or [1.0],
+    )
+    indexer.index_repository()
+
+    result = indexer.index_repository(rebuild=True)
+
+    assert len(embed_calls) == 2
+    assert result["updated_chunks"] == 1
+    assert result["removed_chunks"] == 0
diff --git a/services/local-rag/tests/test_index_jobs.py b/services/local-rag/tests/test_index_jobs.py
--- a/services/local-rag/tests/test_index_jobs.py
+++ b/services/local-rag/tests/test_index_jobs.py
@@ -100,7 +100,7 @@ def configure_api_manager(
     return manager
 
 
-def index_result() -> dict[str, Any]:
+def index_result(**kwargs) -> dict[str, Any]:
     return {
@@ -151,7 +151,7 @@ def test_index_endpoint_records_failure(
     configure_api_manager(monkeypatch, tmp_path)
 
-    def fail_indexing() -> dict[str, Any]:
+    def fail_indexing(**kwargs) -> dict[str, Any]:
         raise RuntimeError("embedding unavailable")
