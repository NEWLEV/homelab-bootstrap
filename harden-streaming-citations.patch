diff --git a/services/local-rag/app/main.py b/services/local-rag/app/main.py
--- a/services/local-rag/app/main.py
+++ b/services/local-rag/app/main.py
@@ -349,6 +349,8 @@ For a supported answer:
 - copy each citation verbatim from a SOURCE bracket above
 - include the complete path and full line range in every citation
 - never shorten [path:line_start-line_end] to [path:line_start]
+- put each citation in its own brackets: [first] [second]
+- never combine sources in one bracket: [first, second]
 
 Repository context:
 
@@ -685,6 +687,10 @@ async def ask_stream(
     request: Request,
     body: AskRequest,
 ) -> StreamingResponse:
+    """Stream provisional tokens followed by an authoritative result.
+
+    Consumers must not treat token events as grounded until the final result.
+    """
     question = body.question.strip()
 
     if not question:
diff --git a/services/local-rag/tests/test_ask.py b/services/local-rag/tests/test_ask.py
--- a/services/local-rag/tests/test_ask.py
+++ b/services/local-rag/tests/test_ask.py
@@ -107,4 +107,6 @@ def test_grounded_prompt_requires_complete_verbatim_citations() -> None:
     assert (
         "never shorten [path:line_start-line_end]"
         in prompt
     )
+    assert "each citation in its own brackets" in prompt
+    assert "never combine sources in one bracket" in prompt
diff --git a/services/local-rag/tests/test_streaming.py b/services/local-rag/tests/test_streaming.py
--- a/services/local-rag/tests/test_streaming.py
+++ b/services/local-rag/tests/test_streaming.py
@@ -209,6 +209,15 @@ def test_disconnected_client_skips_ollama_stream() -> None:
     assert asyncio.run(collect()) == []
 
 
+def test_stream_openapi_marks_tokens_as_provisional() -> None:
+    description = main.app.openapi()["paths"]["/ask/stream"]["post"][
+        "description"
+    ]
+
+    assert "provisional tokens" in description
+    assert "final result" in description
+
+
 def test_stream_limit_and_blank_question_validation() -> None:
     assert client.post(
         "/ask/stream",
